int countKeyChanges(char* s) {
    int p=0;
    for(int i=0;i<strlen(s)-1;i++)
    {
        if(!(s[i]==s[i+1]||s[i]==(char)((int)s[i+1]+32)||s[i]==(char)((int)s[i+1]-32)))
        {
            p++;
        }
        
    }
    return p;
}
